specVersion: "v1beta"
serviceId: "blog-web"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "blog-web-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
